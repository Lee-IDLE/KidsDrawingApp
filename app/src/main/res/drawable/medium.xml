<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:dither="true"
    >
    <!--
        oval: 타원
        dither: 비트맵이 화면과 동일한 픽셀구성을 가지고 있지 않은 경우
        비트맵 디더링을 사용하거나 사용하지 않을 수 있다
        (여기서는 RGB쓰나 화면에서는 ARGB를 쓰고 있다면 정상 작동하도록 함)
    -->
    <size
        android:height="20dp"
        android:width="20dp"/>
    <solid
        android:color="#FF666666"/>

</shape>